%header
  %h1 Log in

= form_for(login_path) do
  .field
    = label_tag(:email)
    = text_field_tag(:email)

  .field
    = label_tag(:password)
    = password_field_tag(:password)

  .actions
    = submit_tag('Log in')
    = link_to('Register', register_path)
