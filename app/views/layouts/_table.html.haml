%table(class="#{table_type}-table")
  %thead
    %tr
      - column_headings.each do |heading|
        %th= "#{heading}".titleize

  %tbody
    - records.each do |record|
      %tr
        %td
          = yield(record)
          = link_to(record.send(columns.first), record)

        - columns[1..-1].each do |column|
          %td= record.send(column)
